{
  "name": "ai-dev-team",
  "version": "0.1.0",
  "description": "Команда агентной разработки ПО",
  "scripts": {
    "mcp": "node ./bin/ai-dev-team-mcp.js",
    "lint:md": "markdownlint '**/*.md' --ignore 'node_modules/**' --ignore 'data/primary/**' --ignore 'data/normalized/**' --ignore 'local/**'"
  },
  "bin": {
    "ai-dev-team": "./bin/ai-dev-team.js",
    "ai-dev-team-mcp": "./bin/ai-dev-team-mcp.js"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.43.0",
    "prettier": "^3.6.2"
  }
}
