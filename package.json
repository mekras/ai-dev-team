{
  "name": "ai-dev-team",
  "version": "0.2.1",
  "description": "Команда агентной разработки ПО",
  "scripts": {
    "lint:md": "markdownlint '**/*.md' --ignore 'node_modules/**' --ignore '.agents/**' --ignore 'apm_modules/**' --ignore 'knowledge/primary/**' --ignore 'knowledge/normalized/**' --ignore 'knowledge/inventory/**' --ignore 'local/**'"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.43.0",
    "prettier": "^3.6.2"
  }
}
