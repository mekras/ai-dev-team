{
  "name": "ai-dev-team",
  "version": "0.11.0",
  "description": "Команда агентной разработки ПО",
  "scripts": {
    "lint:md": "markdownlint '**/*.md' --ignore 'node_modules/**' --ignore 'CLAUDE.md' --ignore '.agents/**' --ignore 'apm_modules/**' --ignore 'knowledge/data/**' --ignore 'knowledge/index/**' --ignore 'local/**'"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.43.0",
    "prettier": "^3.6.2"
  }
}
