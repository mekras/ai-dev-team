# Нормализованное представление: `CODX`

## Основание

- Первичный источник: `data/primary/CODX/source.md`
- Индекс первичных страниц: `data/primary/CODX/page-index.tsv`
- Дата нормализации: `2026-06-03`
- Способ нормализации: извлечение текста из `main` сохранённого HTML,
  удаление `script` и `style`.

## Что сохранено

- `data/normalized/CODX/pages/agents-md/index.md`

## Ограничения

- Извлечение выполнено из одного снимка страницы и не пересматривает
  динамически обновляемую документацию.
- В нормализованном тексте сохранены только смысловые блоки, не все
  служебные детали интерфейса.
