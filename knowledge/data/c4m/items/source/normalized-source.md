# Нормализованное представление: `C4M`

## Основание

- Первичный источник: `knowledge/data/c4m/items/source/source.md`
- Индекс первичных страниц:
  `knowledge/data/c4m/items/primary-c4m-page-index.tsv/page-index.tsv`
- Дата нормализации: `2026-06-05`
- Способ нормализации:
  - выделение покрытия страниц из `page-index.tsv`;
  - фиксация структуры разделов без полного извлечения текста из HTML.

## Что сохранено

- Этот файл как структурированное описание нормализованного слоя.
- Набор страниц методологии, сохранённых в первичном слое:
  - `/introduction`
  - `/history`
  - `/abstractions/*`
  - `/diagrams/*`
  - `/tooling`
  - `/faq`

## Что удалось выделить

- источник охватывает главную страницу и содержательные страницы методологии;
- в корпусе сохранены иерархии разделов `abstractions` и `diagrams`;
- индекс позволяет однозначно связать каждую страницу с локальным HTML-снимком и
  заголовками ответа.

## Ограничения

- Полная нормализация текста всех HTML-страниц в Markdown ещё не выполнена.
- Этот слой пока фиксирует структуру и покрытие источника, а не подробный
  пересказ методологии.
- При содержательных выводах нужно сверяться с первичными HTML-снимками.
